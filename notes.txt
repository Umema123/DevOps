to add on server : to link with server and get push and pull
git remote add https://github.com/Umema123/DevOps.git

add file:
git add filename
git add notes.txt

check status: 
git status

to add path to tracked files
git add path\

commit:
$ git commite -m "everything added"

log: shows history of commit
$ git log

to check all branches:
git branch -r  (on repo)
got branch (on local)

to switch branch:
git checkout branchname

to merge branches: 
git merge branchname                                                 ;q ==> to exit with # terminal 
